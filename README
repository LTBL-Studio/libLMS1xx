=== Introduction

This project contains library for interfacing with SICK LMS1xx lidars.

Authors:
  Konrad Banachowicz

=== POSIX Build instructions

To build this software please do the following:
mkdir build
cd build
cmake  ..
make && make install 

=== Windows Build instructions

On Windows platform, do the following:

cmake .
MSBuild.exe .\libLMS1xx.sln /p:Configuration=Release /p:Platform=x64
cmake -DBUILD_TYPE=Release -P cmake_install.cmake